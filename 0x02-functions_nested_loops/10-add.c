#include "main.h"
/**
 * add - adds two integers and returns the result
 * @s: int to be added to s
 * @d: int to be added to d
 * Return: sum of s and d
 */
int add(int s, int d)
{
int a;

a = s + d;
return (a);
}
