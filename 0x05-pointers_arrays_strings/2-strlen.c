#include "main.h"
/**
 * _strlen - swaps the values of two integers
 * @s: pointer to first value
 * return: length
 */
int _strlen(char *s)
{
int n;

for (n = 0; s[n] != '\0'; n++)

{
    
}
return (n);
}