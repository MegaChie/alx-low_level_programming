#include "main.h"
/**
 * puts_half - swaps the values of two integers
 * @str: pointer to first value
 */
void puts_half(char *str)
{
int n;

for (n = 0; s[n] != '\0'; n++)
{
}
return (n);
if (n % 2 == 0)
{
    int s;
    for (s = n / 2; s <= n; s++)
    {
        _putchar(str[s]);
    }
}
else
{
    int d = n - 1;
    _putchar(str[d]);
}
}
