#include "main.h"
/**
 * puts2 - swaps the values of two integers
 * @str: pointer to first value
 */
void puts2(char *str)
{
int a, s;
for (a = 0; str[a] != '\0'; a = a + 2)
{
}
for (s = 0; s < a; a = a + 2)
{
_putchar(str[s]);
}
_putchar('\n');
}