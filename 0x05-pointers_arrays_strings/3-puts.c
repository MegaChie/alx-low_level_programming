#include "main.h"
/**
 * _puts - swaps the values of two integers
 * @str: pointer to first value
 */
void _puts(char *str)
{
int n;

for (n = 0; str[n] != '\0'; n++)

{

_putchar(s[n]);
}
_putchar('\n');
}