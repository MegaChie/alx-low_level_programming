#include "main.h"
/**
 * print_rev - swaps the values of two integers
 * @s: pointer to first value
 */
void print_rev(char *s)
{
int n, a;
for (n = 0; s[n] != '\0'; n++)

{


}
for (a = n; n != 0; n--)
{
_putchar(s[n]);
}
}