#include "main.h"
/**
 * rev_string - swaps the values of two integers
 * @s: pointer to first value
 */
void rev_string(char *s)
{
int n, a;
for (n = 0; s[n] != '\0'; n++)

{


}
for (a = n - 1; a >= 0; a--)
{
_putchar(s[a]);
}
}