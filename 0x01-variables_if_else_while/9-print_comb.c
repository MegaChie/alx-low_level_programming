#include <stdio.h>
/**
 * main - prints all possible combinations of single-digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;

for (a = 48; n < 58; n++)
	{
	putchar(n);
	if (n != 57)
		{
		putchar(',');
		putchar(' ');
		}
	}
putchart('\n');
return (0);
}
